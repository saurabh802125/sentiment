--extra-index-url https://download.pytorch.org/whl/cpu
--only-binary torch,tokenizers,safetensors,pandas,numpy

Flask==3.0.3
gunicorn==22.0.0
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.32
psycopg2-binary==2.9.9

praw==7.7.1
pygooglenews==0.1.2
feedparser==6.0.11

# HF stack (compatible + wheel-friendly)
transformers==4.45.2
tokenizers==0.20.3
safetensors==0.4.5
huggingface-hub==0.24.6

# PyTorch (CPU wheels via extra index)
torch==2.8.0

# Data deps that match your env (NumPy 2.x available)
pandas==2.2.3
numpy==2.1.3

requests==2.32.3
python-dateutil==2.9.0.post0
